numbers = [1, 2, 3, 4,]
squares = {}
for item in numbers:
    squares[item] = item**2

squares

names = ['Jeremy', 'Kate', 'Patty']
str_lengths = {}
for item in names:
    str_lengths[item] = len(item)
print(str_lengths)

dict1 = {'A':1, 'B':2, 'C':3}
dict2 = {k:v for k,v in dict1.items()}
dict2

student = {'name':'John Doe', 'age':20, 'major':'Computer Science'}
print(student.get('name'))
print(student.get('major'))

car = {"brand": "Toyota", "model": "Corolla", "year": 2020}
car["year"] = 2022
car["color"] = "red"
print(car)

capitals = {"France": "Paris", "Japan": "Tokyo", "India": "New Delhi"}
for item in capitals:
    print(f'The capital of {item} is {capitals[item]}.')

population = {"USA": 331, "Canada": 38, "Brazil": 213, "Germany": 83}
country = input('Enter a country name: ')
if country in population:
    print(f'The population of {country} is: {population[country]}millions.')
else:
    print(f'Country not found.')

course = {"Math":["Jason", "Shanese", "Kelsy"], "Science":["James", "Kelsy", "Andrew"], "Writing":["Walter", "Anna", "Shanese"]}
print(course.get("Math"))


HW
Python Dictionaries and Sets - Questions 
1. Write a program that asks the user to enter 3 student names and their scores.  
Store them in a dictionary and display the dictionary. 
2. Create a dictionary of 5 countries and their capitals in the program.  
Ask the user to enter a country and display its capital (or a message if not found). 
3. Store 3 products and their prices in a dictionary.  
Ask the user to enter a product name and a new price, then update and display the 
dictionary. 
4. Ask the user to enter a sentence.  
Use a dictionary to count how many times each word appears and display the result. 
5. Create two small dictionaries in the program.  
Ask the user if they want to merge them (yes/no). If yes, merge and display the result. 
6. Ask the user to enter numbers for two sets (space-separated).  
Display the numbers that appear in both sets. 
7. Ask the user to enter numbers (space-separated).  
Use a set to remove duplicates and display the unique values. 
8. Ask the user to enter numbers for two sets (space-separated).  
Display the union, intersection, and difference of the sets. 
9. Ask the user to enter 2 student names and their scores.  
Store them in a dictionary and display each student's name and score. 
10. Ask the user to enter a string.  
Display all unique characters using a set and show how many unique characters there are.
Eg for HW (save as sets-dictionaries_solutions.pdf)

Erson's Solutions

1.
dict1 = {'A':1, 'B':2, 'C':3}
dict2 = {k:v for k,v in dict1.items()}
dict2

2.
